% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quaidsce.R
\name{quaidsce}
\alias{quaidsce}
\title{Estimate Censored QUAIDS Demand System}
\usage{
quaidsce(
  data,
  shares,
  prices = NULL,
  lnprices = NULL,
  expenditure = NULL,
  lnexpenditure = NULL,
  demographics = NULL,
  ivexp = NULL,
  anot = 10,
  quadratic = TRUE,
  censor = TRUE,
  method = "ifgnls",
  algorithm = "gn",
  start = "zero",
  initial = NULL,
  sigma_initial = NULL,
  stop_rule = "standard",
  bootstrap_start = "zero",
  first_stage_predict = "xb",
  strict_stata = FALSE,
  vce_sigma = "objective",
  control_function = NULL,
  selection_control_function = NULL,
  selection_prices = NULL,
  selection_covariates = NULL,
  selection_expenditure = TRUE,
  reps = 0L,
  seed = NULL,
  n_jobs = 1L,
  mp_context = "spawn",
  rep_timeout = NULL,
  tol = NULL,
  max_outer = 200L,
  max_iter = 300L,
  chunk = 2000L,
  verbose = FALSE
)
}
\arguments{
\item{data}{A data.frame containing all required variables.}

\item{shares}{Character vector of expenditure share variable names (at least 3).}

\item{prices}{Character vector of price variable names.}

\item{lnprices}{Character vector of log-price variable names (alternative to prices).}

\item{expenditure}{Character name of total expenditure variable.}

\item{lnexpenditure}{Character name of log-expenditure variable.}

\item{demographics}{Optional character vector of demographic variable names.}

\item{ivexp}{Optional character vector of excluded instruments for endogenous
expenditure. The backend regresses log expenditure on log prices,
demographics, these instruments, and a constant; the generated residual enters
both the participation Probits and latent demand equations.}

\item{anot}{Numeric scalar normalization for alpha_0 (default = 10.0).}

\item{quadratic}{Logical, whether to include quadratic expenditure term (default = TRUE).}

\item{censor}{Logical, whether to apply Shonkwiler-Yen censoring correction (default = TRUE).}

\item{method}{Estimation method: 'ifgnls' (default), 'fgnls', or 'nls'.}

\item{algorithm}{Numerical optimizer: 'gn' (Gauss-Newton, default) or 'lm' (Levenberg-Marquardt).}

\item{start}{Starting values: 'zero' (default) or 'linear'.}

\item{initial}{Optional numeric vector of custom starting free parameters (or a previous \code{quaidsce} object).}

\item{sigma_initial}{Optional numeric matrix of custom starting residual covariance Sigma (dimension (n_goods - 1) x (n_goods - 1)).}

\item{stop_rule}{Convergence rule: 'standard' (default) or 'tight'.}

\item{bootstrap_start}{Bootstrap starting scheme: 'zero' (default) or 'warm'.}

\item{first_stage_predict}{First-stage probit predictor: 'xb' (default, theoretical linear index) or 'pr' (legacy Stata-compatible prediction).}

\item{strict_stata}{Logical, whether to match Stata numerical quirks (default = FALSE).}

\item{vce_sigma}{Residual covariance for analytical VCE: 'objective' (default) or 'final'.}

\item{control_function}{Optional character name of endogeneity control function residual.}

\item{selection_control_function}{Optional character name of selection-stage control function.}

\item{selection_prices}{Optional character vector specifying subset of prices in selection probits.}

\item{selection_covariates}{Optional character vector specifying subset of covariates in selection probits.}

\item{selection_expenditure}{Logical, whether expenditure enters selection probits (default = TRUE).}

\item{reps}{Integer, number of bootstrap replications (default = 0).}

\item{seed}{Optional integer, random number seed for bootstrap.}

\item{n_jobs}{Integer, number of parallel bootstrap workers (default = 1).}

\item{mp_context}{Multiprocessing context method (default = "spawn").}

\item{rep_timeout}{Optional numeric timeout per bootstrap replication in seconds.}

\item{tol}{Optional outer convergence tolerance (default = 1e-13). If NULL, default tolerance is used.}

\item{max_outer}{Maximum outer iterations (default = 200).}

\item{max_iter}{Maximum inner iterations (default = 300).}

\item{chunk}{Chunk size for memory management (default = 2000).}

\item{verbose}{Logical, whether to print progress (default = FALSE).}
}
\value{
An object of class \code{quaidsce}.
}
\description{
Estimates a Censored Quadratic Almost Ideal Demand System (QUAIDS) with Ray (1983)
demographic scaling and Shonkwiler & Yen (1999) two-step censoring correction,
powered by the \code{pyquaidsce} Python backend.
}
\examples{
\dontrun{
# Assuming df is a data.frame with budget shares w1..w4 and prices p1..p4
fit <- quaidsce(
  data = df,
  shares = c("w1", "w2", "w3", "w4"),
  prices = c("p1", "p2", "p3", "p4"),
  expenditure = "exp_total",
  demographics = c("hh_size", "urban"),
  anot = 10.0
)
summary(fit)
elasticities(fit)
}
}
